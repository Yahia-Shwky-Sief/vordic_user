import 'package:flutter/material.dart';

logo ({required double fontSize}) => Text(
      'Vordic',
      style: TextStyle(
        fontFamily: 'Dejavu',
        fontSize: fontSize,
        fontWeight: FontWeight.bold,
      ),
    );
